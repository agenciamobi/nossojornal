DirectoryIndex meta.php index.html

<IfModule mod_rewrite.c>
  RewriteEngine On

  RewriteRule ^api/ - [L]

  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  RewriteRule ^ meta.php [L,QSA]
</IfModule>
